#############################################
##### Testing
#############################################

set(prefix "${MODULE}")

add_test(
    NAME ${prefix}_go
    COMMAND ${APPLICATION}
)
set_tests_properties (${prefix}_go PROPERTIES
    LABELS "${MODULE}")

