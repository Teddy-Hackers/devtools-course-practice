#############################################
##### Testing
#############################################

set(prefix "${MODULE}")

add_test(
    NAME ${prefix}_can_Run_Application
    COMMAND ${APPLICATION}
)

set_tests_properties (
    ${prefix}_can_Run_Application PROPERTIES
    LABELS "${MODULE}"
)
